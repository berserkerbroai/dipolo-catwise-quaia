# Dependencias del pipeline del dipolo cosmico.
# Probado con Python 3.11 en un entorno conda (astrolab).
#
#   pip install -r requirements.txt
#
# healpy no tiene ruedas oficiales para Windows. Si estas en Windows,
# instalalo con conda antes de correr pip:
#
#   conda install -c conda-forge healpy
#
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
astropy>=5.3
healpy>=1.16
pyvo>=1.4
